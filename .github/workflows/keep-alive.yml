name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sf https://powerloom-erp-api.onrender.com/api/docs/ || true
